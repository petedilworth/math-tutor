/* Chalk and Paper – connection settings
   Paste the two values from your Supabase project (Project Settings → API).
   Leave them empty and the site runs on this device only, with no sign-in. */
window.CP_CONFIG = {
  supabaseUrl: "",      /* e.g. "https://abcdefghijklmnop.supabase.co" */
  supabaseAnonKey: ""   /* the long "anon public" key; safe to publish, it can only call the cp_* functions */
};
